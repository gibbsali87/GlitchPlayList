{{> menu id="dashboard"}}

{{#each playlists}}
  <section class="ui segment">
    <h2 class="ui header">
      {{title}}
    </h2>
    <p> Total Duration: {{duration}} </p>
    
    <a href="/playlist/{{id}}" class="far fa-folder-open" size="3X"></a>
    
    <i class="fas fa-trash-alt"><a></a></i>
  </section>
{{/each}}